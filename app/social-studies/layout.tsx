import "../subject-hubs.css"
import "../subject-hubs-refinement.css"

export default function SocialStudiesLayout({ children }: { children: React.ReactNode }) {
  return <div className="grade5-social-studies-hub">{children}</div>
}

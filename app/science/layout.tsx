import "../subject-hubs.css"
import "../subject-hubs-refinement.css"

export default function ScienceLayout({ children }: { children: React.ReactNode }) {
  return <div className="grade5-science-hub">{children}</div>
}

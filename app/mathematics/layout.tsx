import "../subject-hubs.css"
import "../subject-hubs-refinement.css"

export default function MathematicsLayout({ children }: { children: React.ReactNode }) {
  return <div className="grade5-mathematics-hub">{children}</div>
}

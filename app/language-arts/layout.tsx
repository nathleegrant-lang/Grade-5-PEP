import type { ReactNode } from "react"
import "./prototype.css"
import "./section-refinement.css"

export default function LanguageArtsPrototypeLayout({ children }: { children: ReactNode }) {
  return <div className="grade5-language-arts-prototype">{children}</div>
}

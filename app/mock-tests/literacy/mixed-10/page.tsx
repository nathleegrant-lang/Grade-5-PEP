"use client"

import { useCallback, useEffect, useRef, useState } from "react"
import Link from "next/link"
import { ArrowLeft, BookOpen, CheckCircle, ChevronLeft, ChevronRight, Clock, Crown, Flag, Home, Lock, Printer, RotateCcw, XCircle } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Progress } from "@/components/ui/progress"
import { Header } from "@/components/header"
import { Footer } from "@/components/footer"
import { useAuth } from "@/contexts/auth-context"
import { cn } from "@/lib/utils"
import { saveStudentTestResult } from "@/lib/student-test-results"
import { calculateAssessmentScore, prepareAssessment, preparePreview } from "@/lib/assessment-engine"
import { languageArtsMixed10Passage, languageArtsMixed10Questions } from "@/lib/assessments/language-arts-mixed-10"

const FREE_QUESTION_LIMIT = 5
const TEST_SECONDS = 60 * 60

type QuestionType = "reading" | "vocabulary" | "grammar" | "writing"
interface Question { id: number; type: QuestionType; skill: string; question: string; options: string[]; correctAnswer: number; explanation: string }

const passage = languageArtsMixed10Passage

const questions: Question[] = languageArtsMixed10Questions

const sections = [
{type:"reading" as const,label:"Reading Comprehension",note:"Main ideas, details, inference, structure, and theme"},
{type:"vocabulary" as const,label:"Vocabulary & Word Study",note:"Meaning, context, word structure, and figurative language"},
{type:"grammar" as const,label:"Grammar & Language Use",note:"Agreement, tense, punctuation, and sentence structure"},
{type:"writing" as const,label:"Writing Skills",note:"Purpose, evidence, organisation, and revision"}
]

export default function G5LaMix10MockTest(){
 const {isPremium,user,isLoading}=useAuth()
 const [started,setStarted]=useState(false)
 const [showResults,setShowResults]=useState(false)
 const [attemptQuestions,setAttemptQuestions]=useState<Question[]>([])
 const [currentQuestion,setCurrentQuestion]=useState(0)
 const [answers,setAnswers]=useState<(number|null)[]>([])
 const [timeLeft,setTimeLeft]=useState(TEST_SECONDS)
 const submittedRef=useRef(false)
 const questionTopRef=useRef<HTMLElement>(null)
 const totalQuestions=attemptQuestions.length
 const q=attemptQuestions[currentQuestion]
 const buildAttempt=useCallback(()=>isPremium?prepareAssessment(questions):preparePreview(questions,FREE_QUESTION_LIMIT),[isPremium])
 const initialiseAttempt=useCallback(()=>{const prepared=buildAttempt();setAttemptQuestions(prepared);setAnswers(new Array(prepared.length).fill(null));setCurrentQuestion(0);setTimeLeft(TEST_SECONDS);setShowResults(false);submittedRef.current=false},[buildAttempt])
 const startTest=()=>{initialiseAttempt();setStarted(true)}
 const calcScore=useCallback(()=>calculateAssessmentScore(attemptQuestions,answers),[attemptQuestions,answers])
 const scorePct=useCallback(()=>totalQuestions?Math.round(calcScore()/totalQuestions*100):0,[calcScore,totalQuestions])
 const submitTest=useCallback(async()=>{if(submittedRef.current)return;submittedRef.current=true;setShowResults(true);if(!user?.id)return;try{await saveStudentTestResult({parentId:user.id,studentName:user.childName??"Student",grade:"grade5",subject:"Literacy",testName:"Mixed 10",difficulty:"Mixed",score:calcScore(),totalQuestions,percentage:scorePct(),completedAt:new Date().toISOString()})}catch(error){console.error("Failed to save test result:",error)}},[user,calcScore,totalQuestions,scorePct])
 useEffect(()=>{if(!started||showResults)return;const timer=setInterval(()=>setTimeLeft(previous=>{if(previous<=1){clearInterval(timer);void submitTest();return 0}return previous-1}),1000);return()=>clearInterval(timer)},[started,showResults,submitTest])
 useEffect(()=>{if(started&&!showResults)questionTopRef.current?.scrollIntoView({behavior:"smooth",block:"start"})},[currentQuestion,started,showResults])
 const formatTime=(seconds:number)=>`${Math.floor(seconds/60).toString().padStart(2,"0")}:${(seconds%60).toString().padStart(2,"0")}`
 const handleAnswer=(index:number)=>setAnswers(previous=>previous.map((answer,i)=>i===currentQuestion?index:answer))
 const resetTest=()=>{setStarted(false);setShowResults(false);setAttemptQuestions([]);setAnswers([]);setCurrentQuestion(0);setTimeLeft(TEST_SECONDS);submittedRef.current=false}
 const getGrade=()=>{const p=scorePct();if(p>=85)return{grade:"Excellent",color:"text-green-600"};if(p>=70)return{grade:"Good",color:"text-blue-600"};if(p>=50)return{grade:"Fair",color:"text-amber-600"};return{grade:"Needs Improvement",color:"text-red-600"}}
 const getSectionStats=(type:QuestionType)=>{const selected=attemptQuestions.map((question,index)=>({question,index})).filter(item=>item.question.type===type);const correct=selected.filter(item=>answers[item.index]===item.question.correctAnswer).length;const total=selected.length;const percentage=total?Math.round(correct/total*100):0;const rating=percentage>=85?"Excellent":percentage>=70?"Good":percentage>=50?"Fair":"Needs Improvement";const ratingColor=percentage>=85?"text-green-600":percentage>=70?"text-blue-600":percentage>=50?"text-amber-600":"text-red-600";return{correct,total,percentage,rating,ratingColor}}
 const secLabel=(type:QuestionType)=>type==="reading"?"Reading Comprehension":type==="vocabulary"?"Vocabulary & Word Study":type==="grammar"?"Grammar & Language Use":"Writing Skills"
 const secColor=(type:QuestionType)=>type==="reading"?"bg-blue-50 text-blue-700":type==="vocabulary"?"bg-purple-50 text-purple-700":type==="grammar"?"bg-green-50 text-green-700":"bg-amber-50 text-amber-700"

 if(!started)return <div className="min-h-screen bg-gradient-to-b from-sky-50 to-slate-50"><Header/><main className="container mx-auto px-4 py-10"><Link href="/mock-tests/language-arts"><Button variant="ghost" className="mb-6"><ArrowLeft className="mr-2 h-4 w-4"/>Back to Language Arts Mock Tests</Button></Link><Card className="mx-auto max-w-4xl overflow-hidden border-blue-200 shadow-lg"><CardHeader className="bg-blue-50 px-6 py-8 text-center"><BookOpen className="mx-auto mb-4 h-14 w-14 text-blue-600"/><CardTitle className="text-3xl text-blue-800">Language Arts Mixed 10</CardTitle><p className="mt-2 text-slate-600">Grade 5 PEP mixed-level practice covering comprehension, vocabulary, grammar, and writing.</p></CardHeader><CardContent className="space-y-6 p-6">{!isPremium&&!isLoading&&<div className="rounded-lg border border-amber-200 bg-amber-50 p-4"><div className="flex items-start gap-3"><Lock className="mt-1 h-5 w-5 text-amber-600"/><div><p className="font-semibold text-amber-800">Free Preview Mode</p><p className="text-sm text-amber-700">Try {FREE_QUESTION_LIMIT} questions from across the four sections.</p><Link href="/pricing" className="mt-3 inline-block"><Button className="bg-amber-500 hover:bg-amber-600"><Crown className="mr-2 h-4 w-4"/>Upgrade Access</Button></Link></div></div></div>}<section aria-labelledby="what-to-expect"><h2 id="what-to-expect" className="mb-4 text-xl font-semibold text-slate-800">What to Expect</h2><div className="grid gap-3 sm:grid-cols-2">{sections.map(section=><div key={section.type} className="rounded-xl border border-slate-200 bg-white p-4"><p className="font-semibold text-blue-800">{section.label}</p><p className="mt-1 text-sm text-slate-600">{section.note}</p></div>)}</div></section><div className="grid grid-cols-2 gap-4 text-center"><div className="rounded-lg bg-slate-50 p-4"><p className="text-2xl font-bold text-blue-600">{isPremium?40:FREE_QUESTION_LIMIT}</p><p className="text-sm text-slate-600">Questions</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-2xl font-bold text-blue-600">60</p><p className="text-sm text-slate-600">Minutes</p></div></div><Button onClick={startTest} disabled={isLoading} className="w-full bg-blue-600 py-6 text-lg hover:bg-blue-700">{isLoading?"Checking Access...":"Start Test"}</Button></CardContent></Card></main><Footer/></div>

 if(showResults){const sc=calcScore(),pct=scorePct(),{grade,color}=getGrade();const strengths=sections.filter(section=>getSectionStats(section.type).percentage>=70);const needs=sections.filter(section=>getSectionStats(section.type).percentage<70);return <div className="min-h-screen bg-gradient-to-b from-sky-50 to-slate-50"><Header/><main className="container mx-auto px-4 py-10"><Card className="mx-auto max-w-4xl border-blue-200 shadow-lg"><CardHeader className="bg-blue-50 text-center"><CheckCircle className="mx-auto mb-4 h-14 w-14 text-blue-600"/><CardTitle className="text-2xl text-blue-800">Language Arts Test Completed</CardTitle><p className="text-slate-600">Language Arts Mixed 10</p></CardHeader><CardContent className="space-y-6 p-6"><div className="rounded-lg bg-slate-50 p-6 text-center"><p className="text-5xl font-bold text-blue-600">{sc}/{totalQuestions}</p><p className="mt-2 text-slate-600">Questions Correct</p></div><div className="grid grid-cols-1 gap-4 text-center sm:grid-cols-3"><div className="rounded-lg bg-slate-50 p-4"><p className="text-3xl font-bold text-blue-600">{pct}%</p><p className="text-sm text-slate-600">Score</p></div><div className="rounded-lg bg-slate-50 p-4"><p className={cn("text-2xl font-bold",color)}>{grade}</p><p className="text-sm text-slate-600">Performance</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-sm font-semibold">{new Date().toLocaleDateString()}</p><p className="text-sm text-slate-600">Completed</p></div></div><section><h2 className="mb-3 text-lg font-semibold text-slate-800">Section Performance</h2><div className="grid grid-cols-1 gap-4 md:grid-cols-2">{sections.map(section=>{const stats=getSectionStats(section.type);return <div key={section.type} className="rounded-xl border border-blue-100 bg-blue-50 p-4"><p className="font-semibold text-blue-800">{section.label}</p><p className="mt-1 text-sm text-slate-500">{section.note}</p><div className="mt-3 flex justify-between"><span className="text-sm">{stats.correct}/{stats.total} correct</span><span className={cn("text-sm font-semibold",stats.ratingColor)}>{stats.rating}</span></div><Progress value={stats.percentage} className="mt-2 h-2"/></div>})}</div></section><div className="grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-green-200 bg-green-50 p-4"><h2 className="font-semibold text-green-800">Strengths</h2><p className="mt-2 text-sm text-green-700">{strengths.length?strengths.map(section=>section.label).join(", "):"Keep practising. Your next attempt can become your strongest one."}</p></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-4"><h2 className="font-semibold text-amber-800">Needs Improvement</h2><p className="mt-2 text-sm text-amber-700">{needs.length?needs.map(section=>section.label).join(", "):"Excellent balance across every section."}</p></div></div><div className="space-y-4">{attemptQuestions.map((question,index)=>{const correct=answers[index]===question.correctAnswer;return <article key={question.id} className={cn("rounded-lg border-2 p-4",correct?"border-green-200 bg-green-50":"border-red-200 bg-red-50")}><div className="flex gap-3">{correct?<CheckCircle className="mt-1 h-5 w-5 text-green-600"/>:<XCircle className="mt-1 h-5 w-5 text-red-600"/>}<div className="flex-1"><p className="font-semibold">Q{index+1} · <span className="text-blue-700">{question.skill}</span></p><p className="mt-1 text-sm text-slate-700">{question.question}</p><p className="mt-2 text-sm">Your answer: {answers[index]!==null?question.options[answers[index] as number]:"Not answered"}</p><p className="text-sm text-green-700">Correct: {question.options[question.correctAnswer]}</p><p className="mt-1 text-sm"><strong>Explanation:</strong> {question.explanation}</p></div></div></article>})}</div><div className="flex flex-col gap-3 sm:flex-row"><Button onClick={()=>window.print()} className="flex-1 bg-blue-600"><Printer className="mr-2 h-4 w-4"/>Print / Save Report</Button><Button onClick={resetTest} variant="outline" className="flex-1"><RotateCcw className="mr-2 h-4 w-4"/>Try Again</Button><Link href="/mock-tests/language-arts" className="flex-1"><Button variant="outline" className="w-full"><Home className="mr-2 h-4 w-4"/>Back to Tests</Button></Link></div></CardContent></Card></main><Footer/></div>}

 if(!q)return null
 const answeredCount=answers.filter(answer=>answer!==null).length
 return <div className="min-h-screen bg-gradient-to-b from-sky-50 to-slate-50"><Header/><header className="sticky top-0 z-20 bg-blue-800 text-white"><div className="container mx-auto flex items-center justify-between px-4 py-4"><div className="flex items-center gap-3"><BookOpen className="h-8 w-8"/><div><h1 className="font-bold">Language Arts Mixed 10</h1><p className="text-xs text-blue-100">Question {currentQuestion+1} of {totalQuestions}</p></div></div><div className={cn("flex items-center gap-2 rounded-lg px-4 py-2 font-mono text-lg",timeLeft<=300?"bg-red-500":"bg-green-600")} aria-label={`Time remaining ${formatTime(timeLeft)}`}><Clock className="h-5 w-5"/>{formatTime(timeLeft)}</div></div></header><div className="sticky top-[72px] z-10 border-b bg-white shadow-sm"><div className="container mx-auto px-4 py-3"><div className="mb-2 flex justify-between text-sm text-slate-600"><span>{answeredCount}/{totalQuestions} answered</span><span>{Math.round(answeredCount/totalQuestions*100)}% complete</span></div><Progress value={answeredCount/totalQuestions*100} className="h-2"/></div></div><main ref={questionTopRef} className="container mx-auto scroll-mt-36 px-4 py-6"><div className="mx-auto max-w-5xl"><Card className="mb-6 border-blue-200"><CardHeader className="bg-blue-50 py-3"><CardTitle className="flex items-center gap-2 text-base text-blue-800"><BookOpen className="h-4 w-4"/>Reading Passage</CardTitle></CardHeader><CardContent className="max-h-[320px] overflow-y-auto p-5" tabIndex={0} aria-label="Scrollable reading passage"><div className="whitespace-pre-line text-sm leading-7 text-slate-700">{passage}</div></CardContent></Card><Card className="mb-4 min-h-[430px] border-blue-100"><CardHeader className={cn("rounded-t-lg",secColor(q.type))}><div className="flex justify-between gap-4"><span className="text-sm font-semibold uppercase">{q.skill}</span><span className="text-xs uppercase opacity-70">{secLabel(q.type)}</span></div></CardHeader><CardContent className="flex min-h-[360px] flex-col p-6"><p className="mb-6 text-base font-medium leading-relaxed">{q.question}</p><div className="space-y-3" role="radiogroup" aria-label={q.question}>{q.options.map((option,index)=><button key={`${q.id}-${index}`} type="button" role="radio" aria-checked={answers[currentQuestion]===index} onClick={()=>handleAnswer(index)} className={cn("w-full rounded-lg border-2 p-4 text-left transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 focus-visible:ring-offset-2",answers[currentQuestion]===index?"border-blue-600 bg-blue-50":"border-slate-200 hover:border-blue-300 hover:bg-blue-50/50")}><span className="mr-3 font-medium text-blue-700">{String.fromCharCode(65+index)}.</span>{option}</button>)}</div></CardContent></Card><div className="mb-5 flex items-center justify-between rounded-xl border bg-white p-3 shadow-sm"><Button variant="outline" onClick={()=>setCurrentQuestion(value=>value-1)} disabled={currentQuestion===0}><ChevronLeft className="mr-2 h-4 w-4"/>Previous</Button>{currentQuestion===totalQuestions-1?<Button onClick={()=>void submitTest()} className="bg-blue-600"><Flag className="mr-2 h-4 w-4"/>Submit Test</Button>:<Button onClick={()=>setCurrentQuestion(value=>value+1)} className="bg-blue-600">Next<ChevronRight className="ml-2 h-4 w-4"/></Button>}</div><Card><CardHeader className="py-3"><CardTitle className="text-sm text-blue-700">Question Navigator</CardTitle></CardHeader><CardContent><div className="grid grid-cols-5 gap-2 sm:grid-cols-10">{attemptQuestions.map((question,index)=><button key={question.id} type="button" aria-label={`Go to question ${index+1}`} aria-current={currentQuestion===index?"step":undefined} onClick={()=>setCurrentQuestion(index)} className={cn("h-9 w-9 rounded text-sm font-medium focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-600 focus-visible:ring-offset-2",currentQuestion===index?"bg-blue-600 text-white":answers[index]!==null?"bg-blue-100 text-blue-700":"bg-slate-100 text-slate-600 hover:bg-slate-200")}>{index+1}</button>)}</div><div className="mt-4 flex flex-wrap gap-4 text-xs text-slate-500"><span className="flex items-center gap-1"><span className="h-3 w-3 rounded bg-blue-600"/>Current</span><span className="flex items-center gap-1"><span className="h-3 w-3 rounded bg-blue-100"/>Answered</span><span className="flex items-center gap-1"><span className="h-3 w-3 rounded bg-slate-100"/>Unanswered</span></div></CardContent></Card></div></main><Footer/></div>
}
